/world/Error(exception/E, datum/e_src)
	desc = esclines.Add(usrinfo)
	desc = ("  " + line) // Pad any unpadded lines, so they look pretty
	desc = line
	desc = esclines.Add(usrinfo)
	desc = "  (This error will now be silenced for [DisplayTimeText(configured_error_silence_time)])"
